apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory # Let's Encrypt API
    email: amanclash4321@gmail.com
    privateKeySecretRef:
      name: letsencrypt-prod # Stores Let's Encrypt key
    solvers:
      - http01:
          ingress:
            class: nginx # Uses NGINX Ingress
